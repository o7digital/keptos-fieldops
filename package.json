{
  "name": "crm-suites-o7-root",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd api && npm install && npm run build",
    "start": "cd api && npm run start:prod"
  }
}
